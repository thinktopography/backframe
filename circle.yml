machine:
  ruby:
    version: 2.2.2
database:
  override:
    - echo null
deployment:
  master:
    branch: master
    commands:
      - npm run coverage
  release:
    branch: release
    commands:
      - cd ~/backframe; bundle exec rake release
