machine:
  ruby:
    version: 2.2.2
database:
  override:
    - echo null
deployment:
  master:
    branch: master
    commands:
      - cd ~/backframe; bundle exec codeclimate-test-reporter
  release:
    branch: release
    commands:
      - cd ~/backframe; bundle exec rake release
